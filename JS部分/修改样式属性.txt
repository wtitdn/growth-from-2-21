JS修改行内样式属性 element.style行内样式操作
                  element.classname 类名样式操作
                  js的行内样式权重更高 style直接写在类的行内里面
                  类名样式操作则是直接在标签内新加一个类 适用于样式修改较多的情况 如果要保存原来的类 则要将原来的类名一起写上
                  